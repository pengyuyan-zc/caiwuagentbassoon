# ═══════════════════════════════════════════════════════════════════════
# 财务智能体 - Python 依赖
# LangGraph Agent + FastAPI + 财务技能工具
# ═══════════════════════════════════════════════════════════════════════

# ── Agent 框架 ───────────────────────────────────────────────────────
# 安装后使用 LangGraph ReAct Agent，不安装则使用纯 Python ReAct（功能完全兼容）
langgraph>=0.2.0
langgraph-checkpoint>=2.0.0

# ── LLM 调用 ──────────────────────────────────────────────────────────
# 直接调用 DashScope API（兼容 OpenAI 格式）
requests>=2.31.0

# ── Web 服务 ─────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# ── Excel 文件处理 ────────────────────────────────────────────────────
openpyxl>=3.1.0

# ── 环境变量 ─────────────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── 数据处理（可选） ─────────────────────────────────────────────────
pandas>=2.0.0

# ═══════════════════════════════════════════════════════════════════════
# 可选依赖（按需安装）
# ═══════════════════════════════════════════════════════════════════════

# Checkpoint 持久化（生产环境推荐）
# pip install langgraph-checkpoint-sqlite   # SQLite 持久化
# pip install langgraph-checkpoint-postgres  # PostgreSQL 持久化

# RAG 知识增强（启用 RAG 时安装）
# pip install chromadb sentence-transformers faiss-cpu

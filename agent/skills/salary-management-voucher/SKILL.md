---
name: salary-management-voucher
description: 根据金蝶云格式的工资报酬支付申请审批表，自动生成管理帐凭证。按归属利润中心（广东公司 vs 其他）和内部业务类型（承揽/派遣/代理/灵工/助残）自动选择应收/应付科目，生成对应借贷分录。
---

# 工资报酬管理帐凭证生成

当用户请求"生成工资管理帐凭证"、"生成工资报酬管理帐凭证"、"X月工资管理帐凭证"、"工资管理凭证"时，立即使用此 skill。

## 凭证分录规则

### 承揽业务（2-4条分录）
```
借 6401.xx.xx  主营业务成本 = 实发工资（填客户名）
贷 1002 银行存款 = 实发工资（填客户名）

# 若有个税：
借 6401.xx.xx  主营业务成本 = 个税（填客户名，摘要加"个税"）
贷 2221.13 应交税费_代扣个人所得税 = 个税（填客户名）
```

### 派遣 / 代理 / 假外包业务（广东公司）
```
借 2202.02.01 应付账款_代收代付款项_代付工资 = 实发工资（填客户名）
贷 1002 银行存款 = 实发工资（填客户名）

# 若有个税：
借 2202.02.05 应付账款_代收代付款项_代付个税 = 个税（填客户名）
贷 2221.13 应交税费_代扣个人所得税 = 个税（填客户名）
```

### 灵工业务（2条分录）
```
借 2202.02.12 应付账款_代收代付款项_代付经营所得 = 实发工资（填客户名）
贷 1002 银行存款 = 实发工资（填客户名）
```

### 助残业务（2条分录）
```
借 2202.01.05 应付账款_投保申报款项_待分配残疾人残保金费用 = 实发工资
贷 1002 银行存款 = 实发工资（填客户名）
```

### 非广东公司（所有业务类型，2条分录）
```
借 1221.01.03 其他应收款_内部公司往来_代交易往来款 = 实发工资（填客户名）
贷 1002 银行存款 = 实发工资（填客户名）
```

## 业务类型 → 科目映射

| 归属利润中心 | 内部业务类型 | 借方科目 | 贷方科目 |
|------------|------------|---------|---------|
| 广东公司 | 承揽业务 | 6401.04.01 | 1002 |
| 广东公司 | 派遣/代理/假外包 | 2202.02.01 | 1002 |
| 广东公司 | 灵工业务 | 2202.02.12 | 1002 |
| 广东公司 | 助残业务 | 2202.01.05 | 1002 |
| 其他 | 所有类型 | 1221.01.03 | 1002 |

## 摘要生成

摘要 = "付" + 客户 + 月份 + "月工资"

有个税时分录摘要追加"个税"。

## 调用方式

请调用 `agent.py` 中的 `invoke_salary_management_voucher` 函数：

```python
from agent import invoke_salary_management_voucher

result = invoke_salary_management_voucher(
    input_file="工资报酬支付申请.xlsx",   # str 或 Path，审批表 Excel 文件路径
    output_dir="输出目录"                  # 可选，默认为输入文件同目录
)
```

## 依赖文件（skill 目录自包含）

- 凭证模板：`金蝶云凭证明细模板.xlsx` 在 skill 目录或桌面
- 账簿映射：`公司主体账套号.xlsx` 在 skill 目录或桌面
- 客户映射：`客户帐套_管理.xlsx` 在 skill 目录或桌面
- 银行日记账：`银行存款日记账.xlsx` 在 skill 目录或桌面

## 返回格式

```python
{
    'success': bool,
    'output_file': '工资发放管理帐凭证.xlsx',
    'approval_count': 15,   # 审批单数量
    'entry_count': 45,      # 分录数量
    'error_count': 0,       # 错误数量
    'message': '生成完成！共 15 张凭证，45 条分录'
}
```

## 错误处理

- 金额检验 ≠ 0 → 跳过并记录错误
- 客户名称未映射 → 警告并继续生成
- 银行账号未匹配 → 跳过并记录错误
- 所有错误汇总在输出的 Excel 的"错误明细"sheet中
